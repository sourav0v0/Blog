<%- include('partials/header') -%>
<h1>Home Page</h1>
<p><%=startContent%></p>
<%

 post.forEach((item)=>{
   %>
  <h1> <%= item.title.toUpperCase() %></h1>
  <% if(item.body.length >100){%>
  <p><%= item.body.substring(0,100) +" ..."%> <a href='/home/<%= item.title %>'>Read more</a></p>
  <%}else{%>
    <p><%= item.body%></p>
  <%}%>
<% }); %>
<%- include('partials/footer') -%>
